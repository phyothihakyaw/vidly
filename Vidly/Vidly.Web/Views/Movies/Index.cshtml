@model IEnumerable<Movie>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="New" class="btn btn-primary mb-2 mt-2">Add new movie</a>
</p>

@if (!Model.Any())
{
    <p>No movies added yet.</p>
}
else
{
    <table class="table table-hover">
        <thead class="thead-light">
            <tr>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Genre.Name)
                </th>
                <th scope="col">
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Genre.Name)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">
                            <i class="fas fa-edit" data-toggle="tooltip" data-placement="top" title="Edit user"></i>
                        </a> &nbsp; | &nbsp;
                        <a asp-action="Details" asp-route-id="@item.Id">
                            <i class="fas fa-info-circle" data-toggle="tooltip" data-placement="top" title="Go to details"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@section Scripts {
    <script>
        $(function () {
            $('[data-toggle="tooltip"]').tooltip()
        })
    </script>
}